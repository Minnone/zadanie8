{
  "name": "task-manager-pwa",
  "version": "1.0.0",
  "description": "PWA приложение для управления задачами",
  "scripts": {
    "start": "http-server -c-1 -p 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}